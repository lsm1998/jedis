redis为何使用自己实现sds，而不用C字符串？
    1.获取长度高效，C语言的strlen实际上是循环去找结束标记'\0'，算法复杂度为O（n）；
    2.操作更加安全，C语言的strcat就是个坑，容易造成缓冲区溢出；
    3.修改高效，C字符串没有缓冲区，每次修改都是重新分配；
    4.二进制安全，C字符串中的空字符都会被认为是结束标记，二进制文件以字符串存储，出现空字符很常见；
